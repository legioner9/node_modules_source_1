
// assigning a value to a variable

/// set or assign or define
{
    const api = {
        setPort: () => {},
        assignAddress: () => {},
        definePath: () => {},
    };
    console.dir({ api });
}