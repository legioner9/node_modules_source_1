const assert = require('assert'); // assert <- func


try {
    assert.ok(true, 'True is ...)))');
    assert.ok(0, 'O is ...(((');
} catch (e) {
    console.log(e.message);
}
//finally {
//
//}


debugger;